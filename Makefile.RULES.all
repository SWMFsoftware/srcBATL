# Syntax: 
# Conditional in Perl syntax using the variables known by Config.pl
# Compilation rules preceeded with a single TAB character.
# Empty line before next conditional.

# compiling with -O1 or -O2 and nagfor + GNU gcc fails on M1 architecture
$Compiler eq "nagfor" and $OpenACC eq "yes"
	${COMPILE.f90} ${Cflag0} BATL_pass_cell_gpu_parallel.f90
	${COMPILE.f90} ${Cflag0} BATL_pass_cell.f90

